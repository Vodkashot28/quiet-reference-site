name: Build Site

on:
  push:
    branches:
      - Master

jobs:
  build:
    runs-on: self-hosted
    name: Hugo Build
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install Hugo
        run: npm install

      - name: Build site
        run: npm run build

